# SR-FLIPFLOP-
SR Flip-Flop GitHub Description  An SR (Set-Reset) Flip-Flop is a sequential logic circuit used to store one bit of information. It has two inputs, S (Set) and R (Reset), and its output changes according to the input conditions and clock signal.
